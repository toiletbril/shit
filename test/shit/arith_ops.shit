#!/bin/env shit
echo $(( 1 + 2 * 3 - 4 / 2 % 3 ))
echo $(( 1 << 4 | 2 & 3 ^ 5 ))
echo $(( ~0 ))
echo $(( 2 ** 10 ))
echo $(( 5 > 3 ? 10 : 20 ))
echo $(( 1 && 0 || 1 ))
x=7; echo $(( x++ + ++x ))
echo $(( 010 + 0x1f ))
