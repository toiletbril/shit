#!/bin/env shit
file=$TEST_TEMP_DIRECTORY/read-eof
shitbox mkdir -p "$TEST_TEMP_DIRECTORY"
printf abc > "$file"
read x < "$file"
echo "no_newline=[$x] $?"
printf 'def\n' > "$file"
read y < "$file"
echo "with_newline=[$y] $?"
shitbox unlink "$file"
