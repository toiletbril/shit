#!/bin/env shit
# Triggers live in a function that is never called, so the analysis stage
# warns on every line while nothing runs.
lint_only() {
  rm -r "$dir/"
  rm -rf /usr
  mkdir -pm 700 a/b
  exit 999
}
echo done
