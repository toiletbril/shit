#!/bin/env shit
c=1; x=9; r=$((c ? 5 : (x=99))); echo $r $x
c=0; x=9; r=$((c ? (x=77) : 5)); echo $r $x
c=1; x=9; r=$((c ? (x=77) : (x=88))); echo $r $x
c=0; x=9; r=$((c ? (x=77) : (x=88))); echo $r $x
echo $((1 ? 2 ? 3 : 4 : 5))
