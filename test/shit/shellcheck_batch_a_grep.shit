#!/bin/env shit
lint_only() {
  grep *.txt file
  grep '*x' file
  for f in $(cat list); do echo "$f"; done
  for f in $(find . -name x); do echo "$f"; done
  find . | xargs echo
  true | echo discarded
}
echo done
